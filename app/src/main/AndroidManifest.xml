<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.admin.loremy">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activites.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activites.Activity_Menu"/>
        <activity android:name=".activites.Activity_Change_Pass"/>
        <activity android:name=".activites.Activity_Create_New_Acc"/>
        <activity android:name=".activites.Activity_Lost_Pass"/>
        <activity android:name=".activites.Activity_DangNhap_2"/>
    </application>

</manifest>